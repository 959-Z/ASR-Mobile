# Models Placeholder

Put user-provided ASR model files here for local experiments.

This project does not download models automatically. If a model download is needed, ask first.

Model files are ignored by Git.
